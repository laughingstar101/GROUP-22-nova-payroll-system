# About
Final year diploma project of a payroll system with authentication, database storage and responsive ui

# Build instructions 
use this command to run the development build on your local machine
`npm install`
`npm run dev`